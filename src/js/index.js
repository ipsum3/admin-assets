// import 'popper.js'
import 'jquery'
import 'bootstrap'
import './tinymce'

// import log from './log.js'

if (process.env.NODE_ENV !== 'production') {
    console.log('We are in development mode !')
}
